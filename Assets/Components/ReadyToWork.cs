using UnityEngine;

public class ReadyToWork : MonoBehaviour {// flag component dynamically added by MoveInFrontOf system to notify when the player is ready to work (moved in front of) an activity
    // Advice: FYFY component aims to contain only public members (according to Entity-Component-System paradigm).
}