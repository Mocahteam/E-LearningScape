using UnityEngine;

public class ForceMove : MonoBehaviour {// flag component dynamically added by LoginManager system and consumed by MoveInFrontOf system
    // Advice: FYFY component aims to contain only public members (according to Entity-Component-System paradigm).
}