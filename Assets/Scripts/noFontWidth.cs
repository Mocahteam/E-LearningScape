using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class noFontWidth : MonoBehaviour {

	//This component is just here to allow to point all the textMeshPro element which must not be changed at the thickness level
}
